syntax = "proto3";

package ctl;

service CtlService {
    rpc CtlStream (stream RequestStat) returns (stream ReleaseCtl) {}
}

message RequestStat {
    int32 sum = 1;
    int32 count = 2;
}

message ReleaseCtl {
    int64 releaseTime =1 ;
}